/*
a)Write a function that takes two parameters of float type and one character parameter and return the value/result. The two float para, meters are operands and the third parameter is an operator (+, -, *, /) . The function performs the operation and returns the result.

b)Write a program that asks the user to enter a arithmetic expression as  shown below:
	    decimalNumber    operator  decimalNumber
        where operator is either +, -, *, or / .
        The program reads the expression and invokes the function written in part a) above and
shows the result on the screen.

Example:
2.45 + 3.0 = 5.45
*/